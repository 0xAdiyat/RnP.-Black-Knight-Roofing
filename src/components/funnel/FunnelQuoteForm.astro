---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import NoiseBg from '@/components/NoiseBg.astro';
import Button from '@/components/ui/Button.astro';
import RatingStars from '@/components/ui/RatingStars.astro';
import bbbLogo from '@/assets/bbb-logo.png';

// Illastrations
import SingleHome from '@/assets/illustrations/single-family-house.png';
import Townhome from '@/assets/illustrations/townhome.png';
import Commercial from '@/assets/illustrations/comercial-building.png';
import MobileHome from '@/assets/illustrations/mobile-home.png';
import Clock from '@/assets/illustrations/clock.png';
import CalenderMonthApril from '@/assets/illustrations/calender-april-7.png';
import CalenderMonthMay from '@/assets/illustrations/calender-may-7.png';
import QuestionCircle from '@/assets/illustrations/question-circle.png';
import RoofReplacement from '@/assets/illustrations/roof-replacement.png';
import LargeRepair from '@/assets/illustrations/roof-large-repair.png';
import SmallRepair from '@/assets/illustrations/roof-small-repair.png';
import { cn } from '@/lib/utils';

const visualThesis =
  'A dark premium conversion card with confident serif typography, tactile option tiles, and a final booking reveal that feels deliberate instead of bolted on.';
const contentPlan = 'Headline, proof, five-step qualification flow, then booking.';
const interactionThesis =
  'Use selection tiles with instant state feedback, a measured step transition, and a same-step reveal from contact capture into booking.';

const homeTypes = [
  { value: 'Single Family', thubmnail: SingleHome },
  { value: 'Townhome / Duplex', thubmnail: Townhome },
  { value: 'Commercial', thubmnail: Commercial },
  { value: 'Mobile Home', thubmnail: MobileHome },
];

const timelines = [
  { value: 'ASAP', thubmnail: Clock },
  { value: 'This Month', thubmnail: CalenderMonthApril },
  { value: 'Next Month', thubmnail: CalenderMonthMay },
  { value: 'Not Sure', thubmnail: QuestionCircle },
];

const scopes = [
  { value: 'Roof Replacement', thubmnail: RoofReplacement },
  { value: 'Large Repair', thubmnail: LargeRepair },
  { value: 'Small Repair', thubmnail: SmallRepair },
  { value: 'Other', thubmnail: QuestionCircle },
];

const calcomLink = import.meta.env.PUBLIC_CALCOM_LINK || 'tarunsoft/30min';
const googleMapsApiKey = import.meta.env.GOOGLE_MAPS_API_KEY || '';
const googleAutocompleteBiasLat = Number(import.meta.env.GOOGLE_AUTOCOMPLETE_BIAS_LAT || '39.7392');
const googleAutocompleteBiasLng = Number(import.meta.env.GOOGLE_AUTOCOMPLETE_BIAS_LNG || '-104.9903');
const googleAutocompleteBiasRadius = Number(import.meta.env.GOOGLE_AUTOCOMPLETE_BIAS_RADIUS || '160934');
---

<div
  class={cn(
    'from-primary/5 to-primary w-fit min-[820px]:min-w-[820px] max-w-7xl rounded-[1.75rem] bg-linear-to-b p-[10px] shadow-[0_28px_80px_rgba(0,0,0,0.48)]'
  )}
  data-visual-thesis={visualThesis}
  data-content-plan={contentPlan}
  data-interaction-thesis={interactionThesis}
>
  <NoiseBg class="funnel-quote-form rounded-[1.35rem] bg-[#222222] px-6 pt-8 pb-6 sm:px-10">
    <div id="funnel-quote-success" class="hidden flex-col items-center justify-center gap-y-6 py-8 text-center">
      <div class="bg-primary/20 border-primary/40 flex size-16 items-center justify-center rounded-full border">
        <Icon name="circle-tick" class="text-primary size-8" />
      </div>
      <h3 class="text-xl font-semibold text-white">Estimate Request Received</h3>
      <p class="text-sm text-neutral-400">Choose a time below and we&apos;ll confirm the details.</p>
    </div>

    <div id="funnel-quote-form-content" class="space-y-6">
      <div class="flex justify-center">
        <RatingStars rating={5} size={22} fillColor="#F7B715" strokeColor="transparent" />
      </div>

      <h2 class="mx-auto text-center text-[clamp(1.5rem,2rem,2rem)] leading-[1.1] font-normal text-white">
        Get a FREE Instant Roofing Estimate For Your Property
      </h2>

      <div
        class="funnel-quote-error pointer-events-none max-h-0 overflow-hidden text-center text-sm font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
        role="alert"
        aria-hidden="true"
      >
        <div class="pt-1">
          <span class="block">There was a problem with your submission.</span>
          <span class="block">Please review the fields below.</span>
        </div>
      </div>

      <div class="flex items-center justify-center gap-x-2 px-2 sm:px-4">
        {
          Array.from({ length: 5 }).map((_, index) => (
            <>
              <div
                class="funnel-step-dot flex size-10 shrink-0 items-center justify-center rounded-full border border-white/20 text-sm text-white/50 transition-all duration-300 ease-out"
                data-step={index + 1}
                data-state={index === 0 ? 'active' : 'upcoming'}
              >
                <span class="funnel-step-label">{index + 1}</span>
              </div>
              {index < 4 && (
                <div
                  class="funnel-step-line h-px flex-1 bg-white/20 transition-colors duration-300 ease-out"
                  data-after-step={index + 1}
                />
              )}
            </>
          ))
        }
      </div>

      <form
        class="funnel-quote-form-fields"
        novalidate
        data-cal-link={calcomLink}
        data-google-maps-api-key={googleMapsApiKey}
        data-google-bias-lat={googleAutocompleteBiasLat}
        data-google-bias-lng={googleAutocompleteBiasLng}
        data-google-bias-radius={googleAutocompleteBiasRadius}
      >
        <input type="hidden" name="landingPage" value="/roof-quote" />
        <input type="hidden" name="pagePath" value="/roof-quote" />
        <input type="hidden" name="utmSource" value="" />
        <input type="hidden" name="utmMedium" value="" />
        <input type="hidden" name="utmCampaign" value="" />
        <input type="hidden" name="utmContent" value="" />
        <input type="hidden" name="utmTerm" value="" />
        <input type="hidden" name="fbclid" value="" />
        <input type="hidden" name="gclid" value="" />
        <input type="hidden" name="msclkid" value="" />
        <input type="hidden" name="ttclid" value="" />

        <div class="funnel-steps-shell relative overflow-hidden">
          <!-- Home type step -->
          <div class="funnel-step is-active space-y-6" data-step="1" aria-hidden="false">
            <div class="space-y-1 text-center">
              <p class="font-heading text-2xl font-medium text-white">What describes your home?</p>
              <p class="text-white/56">Pick the option that best matches the property.</p>
            </div>

            <div class="space-y-2">
              <div
                class="border-radial grid auto-cols-auto auto-rows-max grid-cols-2 gap-3 rounded-2xl p-2 sm:grid-cols-4"
              >
                {
                  homeTypes.map(option => (
                    <label class="funnel-choice-card group block">
                      <input class="funnel-choice-input" type="radio" name="homeType" value={option.value} required />
                      <span class="funnel-choice-surface">
                        <span class="funnel-choice-icon">
                          <Image
                            src={option.thubmnail}
                            alt={option.value}
                            width={option.thubmnail.width}
                            height={option.thubmnail.height}
                            fetchpriority="high"
                            loading="eager"
                            class="w-[100px]"
                          />
                        </span>
                        <span class="funnel-choice-copy">
                          <span class="funnel-choice-title">{option.value}</span>
                        </span>
                        <span class="funnel-choice-check">
                          <Icon name="tick" class="size-3.5" />
                        </span>
                      </span>
                    </label>
                  ))
                }
              </div>

              <p
                class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                aria-hidden="true"
              >
                <span class="block pt-2">Please select one option.</span>
              </p>
            </div>
          </div>

          <!-- Timeline step -->
          <div class="funnel-step space-y-6" data-step="2" aria-hidden="true" inert>
            <div class="space-y-1 text-center">
              <p class="font-heading text-2xl font-medium text-white">When are you wanting this project completed?</p>
              <p class="text-white/56">This helps us understand urgency and scheduling needs.</p>
            </div>

            <div class="space-y-2">
              <div class="border-radial grid grid-cols-2 gap-3 rounded-2xl p-2 sm:grid-cols-4">
                {
                  timelines.map(option => (
                    <label class="funnel-choice-card group">
                      <input class="funnel-choice-input" type="radio" name="timeline" value={option.value} required />
                      <span class="funnel-choice-surface">
                        <span class="funnel-choice-icon">
                          <Image
                            src={option.thubmnail}
                            alt={option.value}
                            width={option.thubmnail.width}
                            height={option.thubmnail.height}
                            fetchpriority="high"
                            loading="eager"
                            class="aspect-square size-[100px]"
                          />
                        </span>
                        <span class="funnel-choice-copy">
                          <span class="funnel-choice-title">{option.value}</span>
                        </span>
                        <span class="funnel-choice-check">
                          <Icon name="tick" class="size-3.5" />
                        </span>
                      </span>
                    </label>
                  ))
                }
              </div>

              <p
                class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                aria-hidden="true"
              >
                <span class="block pt-2">Please select one option.</span>
              </p>
            </div>
          </div>

          <!-- Roofing Type Step -->
          <section class="funnel-step space-y-6" data-step="3" aria-hidden="true" inert>
            <div class="space-y-1 text-center">
              <p class="font-heading text-2xl font-medium text-white">What is the scope of your roofing project?</p>
              <p class="text-white/56">Choose the closest match for the work you need.</p>
            </div>

            <div class="space-y-2">
              <div class="border-radial grid grid-cols-2 gap-3 rounded-2xl p-2 sm:grid-cols-4">
                {
                  scopes.map(option => (
                    <label class="funnel-choice-card group">
                      <input
                        class="funnel-choice-input"
                        type="radio"
                        name="projectScope"
                        value={option.value}
                        required
                      />
                      <span class="funnel-choice-surface">
                        <span class="funnel-choice-icon">
                          <Image
                            src={option.thubmnail}
                            alt={option.value}
                            width={option.thubmnail.width}
                            height={option.thubmnail.height}
                            fetchpriority="high"
                            loading="eager"
                            class="w-[100px]"
                          />
                        </span>
                        <span class="funnel-choice-copy">
                          <span class="funnel-choice-title">{option.value}</span>
                        </span>
                        <span class="funnel-choice-check">
                          <Icon name="tick" class="size-3.5" />
                        </span>
                      </span>
                    </label>
                  ))
                }
              </div>

              <p
                class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                aria-hidden="true"
              >
                <span class="block pt-2">Please select one option.</span>
              </p>
            </div>
          </section>

          <!-- Address -->
          <section class="funnel-step space-y-6" data-step="4" aria-hidden="true" inert>
            <div class="space-y-1">
              <p class="font-heading text-2xl font-medium text-white">What is the address of this project?</p>
              <p class="text-white/56">Tell us where the property is located.</p>
            </div>

            <div class="border-radial grid gap-4 rounded-2xl p-4">
              <!-- Location -->
              <div>
                <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                  <sup>
                    <Icon name="star-icon" class="text-primary size-3" />
                  </sup>
                  Address
                </label>
                <div class="funnel-field-wrapper funnel-field-wrapper-search from-primary border-radial rounded-xl">
                  <div class="relative">
                    <Icon
                      name="search"
                      class="pointer-events-none absolute top-1/2 left-4 size-5 -translate-y-1/2 text-white/45"
                    />
                    <input
                      type="text"
                      name="addressLookup"
                      placeholder="Colorado, USA"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 py-2 pr-4 pl-12 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                </div>
                <p
                  class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                  aria-hidden="true"
                >
                  <span class="block pt-2">Address is required.</span>
                </p>
              </div>

              <!-- Street -->
              <div>
                <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                  <sup>
                    <Icon name="star-icon" class="text-primary size-3" />
                  </sup>
                  Street Address
                </label>
                <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                  <input
                    type="text"
                    name="streetAddress"
                    placeholder="Address"
                    required
                    class="w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                  />
                </div>
                <p
                  class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                  aria-hidden="true"
                >
                  <span class="block pt-2">Street address is required.</span>
                </p>
              </div>

              <div class="grid grid-cols-1 gap-4 sm:grid-cols-[1.25fr_0.8fr]">
                <!-- City -->
                <div>
                  <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                    <sup>
                      <Icon name="star-icon" class="text-primary size-3" />
                    </sup>
                    City
                  </label>
                  <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                  <p
                    class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                    aria-hidden="true"
                  >
                    <span class="block pt-2">City is required.</span>
                  </p>
                </div>

                <!-- Postal -->
                <div>
                  <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                    <sup>
                      <Icon name="star-icon" class="text-primary size-3" />
                    </sup>
                    Postal code
                  </label>
                  <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                    <input
                      type="text"
                      name="postalCode"
                      placeholder="Postal code"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                  <p
                    class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                    aria-hidden="true"
                  >
                    <span class="block pt-2">Postal code is required.</span>
                  </p>
                </div>
              </div>
            </div>
          </section>

          <!-- Contact Info -->
          <section class="funnel-step space-y-6" data-step="5" aria-hidden="true" inert>
            <div class="funnel-final-panel is-active space-y-4" data-panel="lead">
              <div class="space-y-1 text-center">
                <p class="font-heading text-2xl font-medium text-white">Where should we send your estimate details?</p>
                <p class="text-white/56">Submit your contact info and then choose a time to talk.</p>
              </div>

              <div class="grid gap-4 px-px">
                <div>
                  <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                    <sup>
                      <Icon name="star-icon" class="text-primary size-3" />
                    </sup>
                    Full Name
                  </label>
                  <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                    <input
                      type="text"
                      name="fullName"
                      placeholder="Full Name"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                  <p
                    class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                    aria-hidden="true"
                  >
                    <span class="block pt-2">Full name is required.</span>
                  </p>
                </div>

                <div>
                  <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                    <sup>
                      <Icon name="star-icon" class="text-primary size-3" />
                    </sup>
                    Email
                  </label>
                  <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                  <p
                    class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                    aria-hidden="true"
                  >
                    <span class="block pt-2">A valid email is required.</span>
                  </p>
                </div>

                <div>
                  <label class="mb-2 flex items-center gap-x-1.5 text-sm font-medium text-white">
                    <sup>
                      <Icon name="star-icon" class="text-primary size-3" />
                    </sup>
                    Phone
                  </label>
                  <div class="funnel-field-wrapper from-primary border-radial rounded-xl">
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone"
                      required
                      class="h-11 w-full rounded-[0.70rem] bg-white/5 px-4 py-2 text-base text-white placeholder:text-white/38 focus:outline-none"
                    />
                  </div>
                  <p
                    class="funnel-field-error max-h-0 overflow-hidden text-xs font-semibold tracking-wide text-red-500 opacity-0 transition-all duration-300 ease-out"
                    aria-hidden="true"
                  >
                    <span class="block pt-2">Phone number is required.</span>
                  </p>
                </div>
              </div>
            </div>

            <!-- Booking -->
            <div class="funnel-final-panel hidden space-y-4" data-panel="booking">
              <div class="space-y-2 text-center">
                <p class="text-[1.375rem] font-medium text-white">Book your roofing consultation</p>
                <p class="text-sm leading-6 text-white/56">
                  Choose a time that works for you and we’ll take it from there.
                </p>
              </div>

              <p
                class="funnel-webhook-warning hidden rounded-xl border border-amber-400/30 bg-amber-500/10 px-3 py-2 text-xs leading-5 text-amber-200"
                role="status"
              >
                We could not confirm lead sync after two attempts. You can still book below, and we will reconcile your
                request.
              </p>

              <div class="funnel-cal-shell rounded-[1rem] border border-white/10 bg-black/25 p-4 sm:p-6">
                <div id="cal-inline-container" class="w-full lg:w-[992px]" data-cal-link={calcomLink}></div>
              </div>
            </div>
          </section>
        </div>

        <!-- Cta -->
        <div class="mt-8 flex items-center justify-center gap-x-3">
          <Button
            type="button"
            variant="outline"
            class="funnel-btn-back border-primary hidden h-12 min-w-[120px] justify-center"
          >
            Back
          </Button>
          <Button type="button" class="funnel-btn-next h-12 min-w-[148px] justify-center text-[1rem]">
            <span class="funnel-btn-next-label">Next</span>
            <svg
              class="funnel-btn-next-spinner hidden size-4 animate-spin"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
            >
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
            </svg>
          </Button>
        </div>

        <p class="mx-auto mt-4 max-w-xl text-center text-sm leading-5 font-normal text-balance text-white/60">
          By submitting this form, you agree that Black Knight Roofing may contact you about your request. See our
          <a
            href="/privacy-policy"
            class="text-primary font-semibold underline-offset-4 transition-colors hover:text-white hover:underline"
          >
            Privacy Policy
          </a>
          for details.
        </p>
      </form>

      <div class="flex items-center justify-center gap-x-4 pt-5 sm:gap-x-6">
        <div class="flex items-center gap-x-2">
          <Icon name="google-logo" class="size-4 text-white sm:size-5" />
          <div class="flex flex-col gap-y-[2px] text-[11px] leading-tight sm:text-xs">
            <RatingStars rating={5} size={8} fillColor="#fff" strokeColor="transparent" />
            <span class="font-semibold text-white">5.0 Rating</span>
          </div>
        </div>
        <div class="flex items-center gap-x-2">
          <Icon name="facebook-logo" class="size-4 text-white sm:size-5" />
          <div class="flex flex-col gap-y-[2px] text-[11px] leading-tight sm:text-xs">
            <RatingStars rating={5} size={8} fillColor="#fff" strokeColor="transparent" />
            <span class="font-semibold text-white">5.0 Rating</span>
          </div>
        </div>
        <div class="flex items-center gap-x-2">
          <Image src={bbbLogo} alt="BBB" width={16} height={16} class="size-4 sm:size-5" />
          <div class="flex flex-col gap-y-[2px] text-[11px] leading-tight sm:text-xs">
            <RatingStars rating={5} size={8} fillColor="#fff" strokeColor="transparent" />
            <span class="font-semibold text-white">A+ Rating</span>
          </div>
        </div>
      </div>
    </div>
  </NoiseBg>
</div>

<style>
  .funnel-steps-shell {
    transition: height 320ms ease;
  }

  .funnel-step {
    position: absolute;
    inset: 0;
    width: 100%;
    opacity: 0;
    transform: translateY(14px);
    pointer-events: none;
    transition:
      opacity 280ms ease,
      transform 320ms ease;
  }

  .funnel-step.is-active {
    position: relative;
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .funnel-choice-card {
    display: block;
    cursor: pointer;
    touch-action: manipulation;
    aspect-ratio: 1;
  }

  .funnel-choice-input {
    position: absolute;
    inline-size: 1px;
    block-size: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
  }

  .funnel-choice-surface {
    display: grid;
    grid-template-rows: 1fr auto;
    min-height: 10rem;
    overflow: hidden;
    border-radius: 0.85rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 10px;
    background:
      radial-gradient(circle at top left, rgba(242, 199, 68, 0.13), transparent 45%),
      linear-gradient(180deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.02));
    transition:
      border-color 180ms ease,
      transform 180ms ease,
      background 180ms ease,
      box-shadow 180ms ease;
  }

  .funnel-choice-card:hover .funnel-choice-surface {
    transform: translateY(-1px);
    border-color: rgba(242, 199, 68, 0.42);
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.22);
  }

  .funnel-choice-input:focus-visible + .funnel-choice-surface {
    outline: 2px solid rgba(242, 199, 68, 0.8);
    outline-offset: 2px;
  }

  .funnel-choice-input:checked + .funnel-choice-surface {
    border-color: rgba(242, 199, 68, 0.8);
    background: var(--color-primary);
    box-shadow: inset 0 0 0 1px rgba(242, 199, 68, 0.4);
  }

  .funnel-choice-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 8rem;
    color: #f2c744;
  }

  .funnel-choice-title {
    display: block;
    font-size: 0.875rem;
    line-height: 1.35;
    font-weight: 600;
    color: white;
    text-align: center;
    text-wrap: balance;
  }

  @media (max-width: 768px) {
    .funnel-choice-title {
      font-size: 0.6875rem;
    }
  }

  .funnel-choice-input:checked + .funnel-choice-surface .funnel-choice-title,
  .funnel-choice-input:checked + .funnel-choice-surface .funnel-choice-icon {
    color: var(--color-inverted) !important;
  }

  .funnel-choice-check {
    display: none;
  }

  .funnel-choice-copy {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .funnel-choice-icon :global(svg) {
    width: 3rem;
    height: 3rem;
    color: #f2c744;
  }

  .funnel-field-wrapper-search :global(section) {
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.09), rgba(255, 255, 255, 0.04)),
      linear-gradient(180deg, rgba(0, 0, 0, 0.08), rgba(0, 0, 0, 0.08)), var(--noise-bg, #111);
  }

  .funnel-field-wrapper {
    padding-inline: 1px;
  }

  :global(.pac-container) {
    margin-top: 0.35rem;
    border: 1px solid rgba(242, 199, 68, 0.28);
    border-radius: 0.9rem;
    background: #161616;
    box-shadow: 0 22px 60px rgba(0, 0, 0, 0.45);
    font-family: InterVariable, Inter, Arial, sans-serif;
  }

  :global(.pac-item) {
    border-top-color: rgba(255, 255, 255, 0.08);
    padding: 0.7rem 0.9rem;
    color: rgba(255, 255, 255, 0.88);
    background: transparent;
  }

  :global(.pac-item:hover),
  :global(.pac-item-selected) {
    background: rgba(242, 199, 68, 0.12);
  }

  :global(.pac-item-query) {
    color: #fff;
  }

  /* Embedded calendar frame no longer needed */
</style>

<script>
  // @ts-nocheck
  function loadGoogleMapsPlaces(apiKey) {
    if (!apiKey) return Promise.resolve(null);
    if (window.google?.maps?.places) return Promise.resolve(window.google);
    if (window.__googleMapsPlacesPromise) return window.__googleMapsPlacesPromise;

    window.__googleMapsPlacesPromise = new Promise((resolve, reject) => {
      const existingScript = document.querySelector('script[data-google-maps-places-loader]');
      if (existingScript) {
        existingScript.addEventListener('load', () => resolve(window.google));
        existingScript.addEventListener('error', () => reject(new Error('Google Maps failed to load.')));
        return;
      }

      const script = document.createElement('script');
      script.src = `https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(apiKey)}&libraries=places`;
      script.async = true;
      script.defer = true;
      script.dataset.googleMapsPlacesLoader = 'true';
      script.onload = () => resolve(window.google);
      script.onerror = () => reject(new Error('Google Maps failed to load.'));
      document.head.appendChild(script);
    });

    return window.__googleMapsPlacesPromise;
  }

  function initFunnelQuoteForms() {
    document.querySelectorAll('.funnel-quote-form-fields').forEach(form => {
      if (!(form instanceof HTMLFormElement)) return;
      if (form.dataset.initialized) return;
      form.dataset.initialized = 'true';

      const wrapper = form.closest('.funnel-quote-form');
      if (!(wrapper instanceof HTMLElement)) return;

      const TOTAL_STEPS = 5;
      let currentStep = 1;
      let activeStepObserver = null;
      let bookingVisible = false;
      let calendarInitialized = false;

      const steps = form.querySelectorAll('.funnel-step');
      const dots = wrapper.querySelectorAll('.funnel-step-dot');
      const lines = wrapper.querySelectorAll('.funnel-step-line');
      const btnBack = form.querySelector('.funnel-btn-back');
      const btnNext = form.querySelector('.funnel-btn-next');
      const globalError = wrapper.querySelector('.funnel-quote-error');
      const stepsShell = form.querySelector('.funnel-steps-shell');
      const bookingPanel = form.querySelector('.funnel-final-panel[data-panel="booking"]');
      const leadPanel = form.querySelector('.funnel-final-panel[data-panel="lead"]');
      const webhookWarning = form.querySelector('.funnel-webhook-warning');
      const addressLookupInput = form.elements.namedItem('addressLookup');
      const streetAddressInput = form.elements.namedItem('streetAddress');
      const cityInput = form.elements.namedItem('city');
      const postalCodeInput = form.elements.namedItem('postalCode');
      const googleMapsApiKey = form.dataset.googleMapsApiKey ?? '';
      const googleBiasLat = Number(form.dataset.googleBiasLat ?? '39.7392');
      const googleBiasLng = Number(form.dataset.googleBiasLng ?? '-104.9903');
      const googleBiasRadius = Number(form.dataset.googleBiasRadius ?? '160934');
      const wavebookWebhookUrl =
        'https://services.leadconnectorhq.com/hooks/QubIB9oYi7Q0mBKuLdWY/webhook-trigger/e4986955-4314-4bc1-b067-5ef75ed4ad11';
      const WAVEBOOK_TIMEOUT_MS = 8000;
      const WAVEBOOK_RETRY_DELAY_MS = 700;

      const isAddressComponentFilled = input => input instanceof HTMLInputElement && input.value.trim().length > 0;
      const hasManualAddressFallback = () =>
        isAddressComponentFilled(streetAddressInput) &&
        isAddressComponentFilled(cityInput) &&
        isAddressComponentFilled(postalCodeInput);

      const setAddressSelectionState = (selected, selectedLabel = '') => {
        if (!(addressLookupInput instanceof HTMLInputElement)) return;
        addressLookupInput.dataset.placeSelected = selected ? 'true' : 'false';
        addressLookupInput.dataset.selectedValue = selected ? selectedLabel : '';
      };

      const assignAttributionFields = () => {
        const params = new URLSearchParams(window.location.search);
        const mappings = {
          pagePath: window.location.pathname,
          utmSource: params.get('utm_source') ?? '',
          utmMedium: params.get('utm_medium') ?? '',
          utmCampaign: params.get('utm_campaign') ?? '',
          utmContent: params.get('utm_content') ?? '',
          utmTerm: params.get('utm_term') ?? '',
          fbclid: params.get('fbclid') ?? '',
          gclid: params.get('gclid') ?? '',
          msclkid: params.get('msclkid') ?? '',
          ttclid: params.get('ttclid') ?? '',
        };

        Object.entries(mappings).forEach(([name, value]) => {
          const input = form.elements.namedItem(name);
          if (input instanceof HTMLInputElement) {
            input.value = value;
          }
        });
      };

      const setShellHeight = (targetStep = currentStep) => {
        if (!(stepsShell instanceof HTMLElement)) return;
        const activeStep = form.querySelector(`.funnel-step[data-step="${targetStep}"]`);
        if (!(activeStep instanceof HTMLElement)) return;

        const nextHeight = Math.ceil(Math.max(activeStep.offsetHeight, activeStep.scrollHeight)) + 8;
        stepsShell.style.height = `${nextHeight}px`;
      };

      const observeActiveStep = step => {
        if (activeStepObserver) {
          activeStepObserver.disconnect();
        }

        const activeStep = form.querySelector(`.funnel-step[data-step="${step}"]`);
        if (!(activeStep instanceof HTMLElement) || typeof ResizeObserver === 'undefined') return;

        activeStepObserver = new ResizeObserver(() => {
          setShellHeight(step);
        });

        activeStepObserver.observe(activeStep);
      };

      const toggleMessageVisibility = (element, visible) => {
        if (!(element instanceof HTMLElement)) return;
        element.style.maxHeight = visible ? `${element.scrollHeight}px` : '0px';
        element.style.opacity = visible ? '1' : '0';
        element.setAttribute('aria-hidden', visible ? 'false' : 'true');
      };

      const setGlobalErrorVisibility = visible => {
        toggleMessageVisibility(globalError, visible);
      };

      const setFieldErrorVisibility = (fieldError, visible) => {
        toggleMessageVisibility(fieldError, visible);
      };

      const getFieldElements = input => {
        const fieldWrapper =
          input.closest('.funnel-field-wrapper') ||
          input.closest('.funnel-choice-card')?.querySelector('.funnel-choice-surface');

        // Most fields have the error message as the immediate sibling of the wrapper
        let fieldError = fieldWrapper?.nextElementSibling?.classList.contains('funnel-field-error')
          ? fieldWrapper.nextElementSibling
          : null;

        // Fallback: search the closest container (useful for radio groups/choice cards)
        if (!fieldError) {
          const container = input.closest('.funnel-choice-card')?.parentElement || input.closest('.funnel-step');
          fieldError = container?.querySelector('.funnel-field-error');
        }

        return { fieldError, fieldWrapper };
      };

      const getFieldValidationMessage = input => {
        const value = input.value.trim();

        // 1. Radio buttons (Step 1-3)
        if (input.type === 'radio') {
          const checked = form.querySelector(`input[name="${input.name}"]:checked`);
          return checked ? '' : 'Please select an option.';
        }

        // 2. Fetch label or placeholder for the field to create friendly messages
        const fieldGroup = input.closest('.funnel-choice-card') || input.parentElement?.parentElement;
        const labelText = fieldGroup?.querySelector('label')?.textContent.replace(/[*:]/g, '').trim();
        const fieldName = labelText || input.placeholder || 'This field';

        // 3. Handle empty state for required fields
        if (!value && input.hasAttribute('required')) {
          return `${fieldName} is required.`;
        }

        // 4. Specific validation for non-empty but invalid fields
        if (input.name === 'postalCode') {
          const digits = value.replace(/\D/g, '');
          return digits.length >= 5 ? '' : 'Please enter a valid postal code.';
        }

        if (input.name === 'addressLookup') {
          const placeSelected = input.dataset.placeSelected === 'true';
          return placeSelected || hasManualAddressFallback()
            ? ''
            : 'Please select an address from the list or complete the fields below.';
        }

        if (input.name === 'streetAddress' && value.length < 5) {
          return 'Please enter a full street address.';
        }

        if (input.name === 'city' && value.length < 2) {
          return 'Please enter a valid city name.';
        }

        if (input.name === 'phone') {
          const digits = value.replace(/\D/g, '');
          return digits.length >= 10 ? '' : 'Please enter a valid 10-digit phone number.';
        }

        if (input.type === 'email') {
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          return emailRegex.test(value) ? '' : 'Please enter a valid email address.';
        }

        return '';
      };

      const updateFieldState = (input, showError = false) => {
        const { fieldError, fieldWrapper } = getFieldElements(input);
        const message = getFieldValidationMessage(input);
        const isValid = message === '';

        if (fieldError instanceof HTMLElement) {
          const content = fieldError.querySelector('span');
          if (content) content.textContent = message;
          setFieldErrorVisibility(fieldError, showError && !isValid);
        }

        if (input.type === 'radio') {
          const cards = form.querySelectorAll(`input[name="${input.name}"]`);
          cards.forEach(radio => {
            const surface = radio.closest('.funnel-choice-card')?.querySelector('.funnel-choice-surface');
            surface?.classList.toggle('ring-1', showError && !isValid);
            surface?.classList.toggle('ring-red-500', showError && !isValid);
          });
        } else if (fieldWrapper instanceof HTMLElement) {
          fieldWrapper.classList.toggle('ring-1', !isValid && showError);
          fieldWrapper.classList.toggle('ring-red-500', !isValid && showError);
        }

        return isValid;
      };

      const clearErrors = () => {
        setGlobalErrorVisibility(false);

        form.querySelectorAll('.funnel-field-error').forEach(el => {
          setFieldErrorVisibility(el, false);
        });

        form.querySelectorAll('.funnel-field-wrapper, .funnel-choice-surface').forEach(el => {
          el.classList.remove('ring-1', 'ring-red-500');
        });

        requestAnimationFrame(() => setShellHeight());
      };

      const updateAddressFieldsValidity = showError => {
        if (addressLookupInput instanceof HTMLInputElement) updateFieldState(addressLookupInput, showError);
        if (streetAddressInput instanceof HTMLInputElement) updateFieldState(streetAddressInput, showError);
        if (cityInput instanceof HTMLInputElement) updateFieldState(cityInput, showError);
        if (postalCodeInput instanceof HTMLInputElement) updateFieldState(postalCodeInput, showError);

        const addressStep = addressLookupInput?.closest('.funnel-step');
        const hasStepErrors = addressStep?.querySelector('.funnel-field-error[aria-hidden="false"]');
        setGlobalErrorVisibility(Boolean(hasStepErrors));
        requestAnimationFrame(() => setShellHeight());
      };

      const getAddressComponent = (components, types) => {
        if (!Array.isArray(components)) return '';
        const matched = components.find(component => types.every(type => component.types?.includes(type)));
        return matched?.long_name ?? '';
      };

      const populateAddressFromPlace = place => {
        const components = place?.address_components;
        if (!Array.isArray(components)) return false;

        const streetNumber = getAddressComponent(components, ['street_number']);
        const route = getAddressComponent(components, ['route']);
        const city =
          getAddressComponent(components, ['locality']) ||
          getAddressComponent(components, ['postal_town']) ||
          getAddressComponent(components, ['sublocality_level_1']) ||
          getAddressComponent(components, ['administrative_area_level_2']);
        const postalCode = getAddressComponent(components, ['postal_code']);
        const postalCodeSuffix = getAddressComponent(components, ['postal_code_suffix']);

        if (!(streetAddressInput instanceof HTMLInputElement)) return false;
        if (!(cityInput instanceof HTMLInputElement)) return false;
        if (!(postalCodeInput instanceof HTMLInputElement)) return false;
        if (!(addressLookupInput instanceof HTMLInputElement)) return false;

        streetAddressInput.value = [streetNumber, route].filter(Boolean).join(' ').trim();
        cityInput.value = city;
        postalCodeInput.value = postalCodeSuffix ? `${postalCode}-${postalCodeSuffix}` : postalCode;
        addressLookupInput.value = place.formatted_address || place.name || addressLookupInput.value;
        setAddressSelectionState(true, addressLookupInput.value.trim());
        updateAddressFieldsValidity(false);
        return true;
      };

      const initAddressAutocomplete = async () => {
        if (!(addressLookupInput instanceof HTMLInputElement)) return;
        if (!googleMapsApiKey) return;

        try {
          await loadGoogleMapsPlaces(googleMapsApiKey);
        } catch (error) {
          console.error(error);
          return;
        }

        if (!window.google?.maps?.places) return;
        if (addressLookupInput.dataset.autocompleteInitialized === 'true') return;

        const autocomplete = new window.google.maps.places.Autocomplete(addressLookupInput, {
          componentRestrictions: { country: ['us'] },
          fields: ['address_components', 'formatted_address', 'name'],
          types: ['address'],
        });

        autocomplete.setBounds(
          new window.google.maps.Circle({
            center: { lat: googleBiasLat, lng: googleBiasLng },
            radius: googleBiasRadius,
          }).getBounds()
        );
        autocomplete.setOptions({ strictBounds: false });

        addressLookupInput.dataset.autocompleteInitialized = 'true';
        setAddressSelectionState(false);

        autocomplete.addListener('place_changed', () => {
          const place = autocomplete.getPlace();
          const populated = populateAddressFromPlace(place);

          if (!populated) {
            setAddressSelectionState(false);
            updateAddressFieldsValidity(true);
          }
        });

        addressLookupInput.addEventListener('input', () => {
          if (!(addressLookupInput instanceof HTMLInputElement)) return;

          const selectedValue = addressLookupInput.dataset.selectedValue ?? '';
          if (!selectedValue || addressLookupInput.value.trim() !== selectedValue) {
            setAddressSelectionState(false);
          }
        });
      };

      const validateStep = step => {
        const currentPanel = form.querySelector(`.funnel-step[data-step="${step}"]`);
        if (!(currentPanel instanceof HTMLElement)) return true;

        clearErrors();
        let valid = true;

        const requiredInputs = Array.from(currentPanel.querySelectorAll('input[required]'));
        const radioNames = new Set(requiredInputs.filter(input => input.type === 'radio').map(input => input.name));

        radioNames.forEach(name => {
          const representative = currentPanel.querySelector(`input[name="${name}"]`);
          const groupValid = updateFieldState(representative, true);
          if (!groupValid) valid = false;
        });

        currentPanel.querySelectorAll('input[required]:not([type="radio"]), select[required]').forEach(input => {
          const fieldValid = updateFieldState(input, true);
          if (!fieldValid) valid = false;
        });

        if (!valid) {
          setGlobalErrorVisibility(true);
        }

        requestAnimationFrame(() => setShellHeight(step));
        return valid;
      };

      const validateStepsThrough = finalStep => {
        for (let step = 1; step <= finalStep; step += 1) {
          const valid = validateStep(step);
          if (!valid) {
            showStep(step);
            return false;
          }
        }
        showStep(finalStep);
        return true;
      };

      const wait = delayMs =>
        new Promise(resolve => {
          window.setTimeout(resolve, delayMs);
        });

      const postWavebookPayload = async payload => {
        const controller = new AbortController();
        const timeoutId = window.setTimeout(() => controller.abort(), WAVEBOOK_TIMEOUT_MS);

        try {
          const response = await fetch(wavebookWebhookUrl, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload),
            signal: controller.signal,
          });

          if (!response.ok) {
            throw new Error(`Webhook submission failed with status ${response.status}`);
          }
        } finally {
          window.clearTimeout(timeoutId);
        }
      };

      const getFormPayload = () => {
        const formData = new FormData(form);
        const values = {};

        formData.forEach((value, key) => {
          if (typeof value === 'string') {
            values[key] = value.trim();
          }
        });

        const [firstName = '', ...restNameParts] = (values.fullName || '').split(/\s+/).filter(Boolean);
        const lastName = restNameParts.join(' ');
        const queryParams = new URLSearchParams(window.location.search);

        return {
          ...values,
          firstName,
          lastName,
          submittedAt: new Date().toISOString(),
          timezone: Intl.DateTimeFormat().resolvedOptions().timeZone || '',
          referrer: document.referrer || '',
          userAgent: navigator.userAgent || '',
          currentUrl: window.location.href,
          queryString: window.location.search || '',
          funnelName: 'roof_quote_funnel',
          formName: 'roof_quote_step_5',
          funnelStep: 5,
          leadStatus: 'submitted_before_booking',
          submissionChannel: 'wavebook_webhook',
          fbclid: values.fbclid || queryParams.get('fbclid') || '',
          gclid: values.gclid || queryParams.get('gclid') || '',
          msclkid: values.msclkid || queryParams.get('msclkid') || '',
          ttclid: values.ttclid || queryParams.get('ttclid') || '',
        };
      };

      const buildCalPrefill = payload => {
        const line = (label, value) => (value ? `${label}: ${value}` : '');
        const notesLines = [
          line('Home Type', payload.homeType),
          line('Project Timeline', payload.timeline),
          line('Project Scope', payload.projectScope),
          line('Project Address', [payload.streetAddress, payload.city, payload.postalCode].filter(Boolean).join(', ')),
          line('Phone', payload.phone),
        ].filter(Boolean);

        return {
          name: payload.fullName || '',
          email: payload.email || '',
          notes: notesLines.join('\n'),
        };
      };

      const submitToWavebookWithRetry = async payload => {
        try {
          await postWavebookPayload(payload);
          return true;
        } catch (error) {
          console.error('Wavebook webhook attempt 1 failed:', error);
        }

        await wait(WAVEBOOK_RETRY_DELAY_MS);

        try {
          await postWavebookPayload(payload);
          return true;
        } catch (error) {
          console.error('Wavebook webhook attempt 2 failed:', error);
          return false;
        }
      };

      const ensureCalLoaded = () =>
        new Promise(resolve => {
          if (!window.Cal) {
            (function (C, A, L) {
              let p = function (a, ar) {
                a.q.push(ar);
              };
              let d = C.document;
              C.Cal =
                C.Cal ||
                function () {
                  let cal = C.Cal;
                  let ar = arguments;
                  if (!cal.loaded) {
                    cal.ns = {};
                    cal.q = cal.q || [];
                    let s = d.createElement('script');
                    s.src = A;
                    d.head.appendChild(s);
                    cal.loaded = true;
                  }
                  if (ar[0] === L) {
                    const api = function () {
                      p(api, arguments);
                    };
                    const namespace = ar[1];
                    api.q = api.q || [];
                    typeof namespace === 'string' ? (cal.ns[namespace] = cal.ns[namespace] || api) : p(cal, ar);
                    return;
                  }
                  p(cal, ar);
                };
            })(window, 'https://app.cal.com/embed/embed.js', 'init');
          }

          window.Cal('init', '30min', { origin: 'https://app.cal.com' });

          window.Cal.ns['30min']('ui', {
            theme: 'dark',
            cssVarsPerTheme: {
              light: { 'cal-brand': '#0B2B26' },
              dark: { 'cal-brand': '#C2E800' },
            },
            hideEventTypeDetails: false,
            layout: 'month_view',
          });

          // Inline initialization moved to revealBookingPanel for better DOM timing.
          // We intentionally do not override `bookingSuccessful`, so Cal's native
          // inline success view remains visible after booking.

          resolve();
        });

      const revealBookingPanel = async payload => {
        if (!(leadPanel instanceof HTMLElement) || !(bookingPanel instanceof HTMLElement)) return;

        leadPanel.classList.remove('is-active');
        leadPanel.classList.add('hidden');
        bookingPanel.classList.remove('hidden');
        bookingPanel.classList.add('is-active');
        bookingVisible = true;

        if (btnNext instanceof HTMLElement) btnNext.classList.add('hidden');
        if (btnBack instanceof HTMLElement) btnBack.classList.add('hidden');

        if (!calendarInitialized) {
          await ensureCalLoaded();
          const calPrefill = buildCalPrefill(payload ?? {});

          const target = wrapper.querySelector('#cal-inline-container');
          if (target && window.Cal?.ns?.['30min']) {
            window.Cal.ns['30min']('inline', {
              elementOrSelector: target,
              calLink: form.dataset.calLink || 'tarunsoft/30min',
              layout: 'month_view',
              prefill: calPrefill,
              config: {
                layout: 'month_view',
                theme: 'dark',
                name: calPrefill.name,
                email: calPrefill.email,
                notes: calPrefill.notes,
              },
            });
          }
          calendarInitialized = true;
        }

        if (btnNext) btnNext.style.display = 'none';
        if (btnBack) btnBack.style.display = 'none';

        requestAnimationFrame(() => setShellHeight(5));
      };

      const showStep = step => {
        steps.forEach(el => {
          const stepNumber = Number(el.dataset.step);
          const isActive = stepNumber === step;
          el.classList.toggle('is-active', isActive);
          el.setAttribute('aria-hidden', isActive ? 'false' : 'true');
          if (isActive) {
            el.removeAttribute('inert');
          } else {
            el.setAttribute('inert', '');
          }
        });

        dots.forEach(dot => {
          const stepNumber = Number(dot.dataset.step);
          const label = dot.querySelector('.funnel-step-label');
          const isCompleted = stepNumber < step;
          const isActive = stepNumber === step;

          if (isCompleted) {
            dot.dataset.state = 'complete';
            dot.className =
              'funnel-step-dot bg-primary flex size-10 shrink-0 items-center justify-center rounded-full text-sm font-bold text-black transition-all duration-300 ease-out';
            if (label) label.textContent = '✓';
          } else if (isActive) {
            dot.dataset.state = 'active';
            dot.className =
              'funnel-step-dot bg-primary flex size-10 shrink-0 items-center justify-center rounded-full text-sm font-bold text-black transition-all duration-300 ease-out';
            if (label) label.textContent = String(stepNumber);
          } else {
            dot.dataset.state = 'upcoming';
            dot.className =
              'funnel-step-dot flex size-10 shrink-0 items-center justify-center rounded-full border border-white/20 text-sm text-white/50 transition-all duration-300 ease-out';
            if (label) label.textContent = String(stepNumber);
          }
        });

        lines.forEach(line => {
          const afterStep = Number(line.dataset.afterStep);
          if (afterStep < step) {
            line.classList.remove('bg-white/20');
            line.classList.add('bg-primary/40');
          } else {
            line.classList.remove('bg-primary/40');
            line.classList.add('bg-white/20');
          }
        });

        if (btnBack instanceof HTMLElement) {
          btnBack.classList.toggle('hidden', step === 1 || bookingVisible);
        }

        if (btnNext instanceof HTMLElement) {
          btnNext.classList.remove('hidden');
          const label = btnNext.querySelector('.funnel-btn-next-label');
          if (label) {
            label.textContent = step === 5 ? 'Submit' : 'Next';
          }
        }

        currentStep = step;
        observeActiveStep(step);
        requestAnimationFrame(() => setShellHeight(step));
      };

      const handleFinalStepSubmit = async () => {
        if (!validateStepsThrough(5)) return;

        if (webhookWarning instanceof HTMLElement) {
          webhookWarning.classList.add('hidden');
        }

        if (btnNext instanceof HTMLButtonElement) {
          btnNext.disabled = true;
          const label = btnNext.querySelector('.funnel-btn-next-label');
          const spinner = btnNext.querySelector('.funnel-btn-next-spinner');
          if (label) label.textContent = 'Submitting...';
          if (spinner) spinner.classList.remove('hidden');
        }

        try {
          const payload = getFormPayload();
          const webhookSuccess = await submitToWavebookWithRetry(payload);
          await revealBookingPanel(payload);

          if (!webhookSuccess && webhookWarning instanceof HTMLElement) {
            webhookWarning.classList.remove('hidden');
          }
        } catch (error) {
          console.error(error);
          if (btnNext instanceof HTMLButtonElement) {
            btnNext.disabled = false;
            const label = btnNext.querySelector('.funnel-btn-next-label');
            const spinner = btnNext.querySelector('.funnel-btn-next-spinner');
            if (label) label.textContent = 'Submit';
            if (spinner) spinner.classList.add('hidden');
          }
          alert('There was a problem opening the calendar. Please try again.');
        }
      };

      btnNext?.addEventListener('click', () => {
        if (!validateStep(currentStep)) return;

        if (currentStep < TOTAL_STEPS) {
          showStep(currentStep + 1);
          return;
        }

        handleFinalStepSubmit();
      });

      btnBack?.addEventListener('click', () => {
        clearErrors();
        if (currentStep > 1) {
          showStep(currentStep - 1);
        }
      });

      form.querySelectorAll('input[required], select[required]').forEach(input => {
        const validateCurrentField = () => {
          if (input.type === 'radio') {
            updateFieldState(input, false);
            return;
          }

          const hasVisibleError =
            form.querySelector('.funnel-quote-error')?.getAttribute('aria-hidden') === 'false' ||
            input.closest('.funnel-step')?.querySelector('.funnel-field-error[aria-hidden="false"]');

          updateFieldState(input, Boolean(hasVisibleError));

          const currentPanel = input.closest('.funnel-step');
          const hasStepErrors = currentPanel?.querySelector('.funnel-field-error[aria-hidden="false"]');
          setGlobalErrorVisibility(Boolean(hasStepErrors));
          requestAnimationFrame(() => setShellHeight());
        };

        const eventName = input.type === 'radio' ? 'change' : 'input';
        input.addEventListener(eventName, validateCurrentField);

        // Auto-forwarding for steps 1, 2, and 3
        if (input.type === 'radio') {
          input.addEventListener('change', () => {
            const stepEl = input.closest('.funnel-step');
            const stepNumber = stepEl ? parseInt(stepEl.dataset.step) : 0;
            if (stepNumber >= 1 && stepNumber <= 3) {
              setTimeout(() => {
                if (currentStep === stepNumber) {
                  btnNext?.click();
                }
              }, 320);
            }
          });
        }
        input.addEventListener('blur', () => {
          if (input.type === 'radio') return;
          updateFieldState(input, true);
          const currentPanel = input.closest('.funnel-step');
          const hasStepErrors = currentPanel?.querySelector('.funnel-field-error[aria-hidden="false"]');
          setGlobalErrorVisibility(Boolean(hasStepErrors));
          requestAnimationFrame(() => setShellHeight());
        });
      });

      assignAttributionFields();
      void initAddressAutocomplete();
      window.addEventListener('resize', () => setShellHeight(currentStep));
      showStep(1);
      requestAnimationFrame(() => setShellHeight(1));
    });
  }

  initFunnelQuoteForms();
  document.addEventListener('astro:after-swap', initFunnelQuoteForms);
</script>
