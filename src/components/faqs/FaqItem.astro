---
import { cn } from '@/lib/utils';

interface Props {
  question: string;
  answer: string;
  open?: boolean;
}

const { question, answer, open = false } = Astro.props;
---

<faq-item
  data-open={open.toString()}
  class={cn(
    'border-gradient before:via-primary/30 before:pt-0 group/faq-item before:bg-linear-to-r before:from-transparent rounded-2xl before:via-50% before:to-transparent data-[open=true]:before:pt-px data-[open=true]:before:from-[#F2C74422] data-[open=true]:before:via-primary data-[open=true]:before:to-[#F2C74422] transition-all duration-200 ease-in-out'
  )}
>
  <div class={cn('bg-[#171717] lg:bg-bg-default block p-5 rounded-2xl', open && 'rounded-2xl')}>
    <button
      class="flex w-full items-center justify-between gap-x-4 text-left hover:cursor-pointer"
      aria-expanded={open}
    >
      <div class="text-base">{question}</div>
      <div class="from-primary rounded-full bg-radial-[at_25%_25%] via-[#594216] via-50% to-[#594216] p-px">
        <div
          class="flex size-8 shrink-0 items-center justify-center rounded-full bg-[#303030] transition-colors duration-300"
        >
          <svg
            class="faq-icon-plus transition-transform duration-300"
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M5 12h14"></path>
            <path class="faq-icon-vertical" d="M12 5v14"></path>
          </svg>
        </div>
      </div>
    </button>
    <div class="faq-answer">
      <div class="pt-4 text-sm leading-relaxed text-white/60">
        {answer}
      </div>
    </div>
  </div>
</faq-item>

<style>
  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  faq-item[data-open='true'] .faq-answer {
    max-height: 500px;
  }

  faq-item[data-open='true'] .faq-icon-vertical {
    transform: rotate(90deg);
    opacity: 0;
  }

  .faq-icon-vertical {
    transition:
      transform 0.3s ease,
      opacity 0.3s ease;
  }
</style>

<script>
  class FaqItem extends HTMLElement {
    constructor() {
      super();

      const button = this.querySelector('button');
      button?.addEventListener('click', () => {
        const isOpen = this.dataset.open === 'true';
        this.dataset.open = (!isOpen).toString();
        button.setAttribute('aria-expanded', (!isOpen).toString());

        // Update icon container style
        const iconContainer = button.querySelector('span:last-child') as HTMLElement;
        if (iconContainer) {
          if (!isOpen) {
            iconContainer.classList.remove('border-[#555]', 'bg-transparent');
            iconContainer.classList.add('border-primary', 'bg-primary/10');
          } else {
            iconContainer.classList.remove('border-primary', 'bg-primary/10');
            iconContainer.classList.add('border-[#555]', 'bg-transparent');
          }
        }
      });
    }
  }

  customElements.define('faq-item', FaqItem);
</script>
